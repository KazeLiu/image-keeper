# ImageKeeper 基准图片对比与安全去重流程规范

> 文档状态：目标设计规范（Target Specification）  
> 适用版本：ImageKeeper v1.x 规划实现  
> 最后更新：2026-07-13  
> 本文描述期望行为和验收边界，不表示当前代码已经全部实现。

## 1. 文档目标

本文定义 ImageKeeper 的多目录图片对比、候选识别、人工裁决和安全回收流程，并作为后续实现、测试和代码审查的依据。

核心业务模型：

```text
A：baselineRoot，受保护的基准目录
B/C/...：comparisonRoots，待整理的一个或多个对比目录

只执行：comparisonRoot → A
默认不执行：B ↔ C、B ↔ B、C ↔ C
```

因此，本流程回答的是“对比目录中的图片是否已经存在于基准 A，或是否可能是 A 中图片的低质量版本/差分版本”，不是全图库去重。

### 1.1 目标

- 识别字节完全相同的文件。
- 召回可能经过缩放、压缩或重新编码的图片。
- 发现同一主题下需要保留的差分图或变体图。
- 让每个对比目录文件得到明确、可解释、可追踪的分析结果。
- 在文件发生变化、算法不确定或证据不足时保守保留。
- 分析结果触发的首次文件操作可恢复、可审计，并且不会修改基准目录；永久删除作为独立的最终操作处理。

### 1.2 非目标

- 默认不判断 B、C 之间是否重复。
- 默认不整理或删除 A 内部的重复文件。
- 不把文件大小、扩展名或单一相似度分数当作图片质量的充分证明。
- 不承诺仅凭算法自动识别所有裁剪、拼接、旋转、水印或大幅改色图片。
- 不在分析完成后直接永久删除原文件。

## 2. 安全不变量

以下规则优先级高于性能优化、用户自定义阈值和批量操作：

1. **A 永远只读**：扫描、索引、预览和报告可以读取 A；回收、移动、重命名和删除操作不得以 A 中的文件为目标。
2. **不确定即保留**：错误、未计算、结果过期、候选被截断或多候选冲突时，不得产生可执行的删除动作。
3. **分析与操作分离**：算法只生成分析类别和建议；文件操作需要独立的审核状态。
4. **永久删除与分析分离**：分析结果最多允许把文件移动到 ImageKeeper 回收站；永久删除只能在回收站页面单独执行并二次确认。
5. **执行前重新验证**：移动到回收站前必须重新验证对比文件和所依赖的 A 文件，不能直接使用过期扫描结果。
6. **每个结果可追溯**：必须能还原使用的目录、文件指纹、算法版本、阈值、候选证据、人工决定和操作结果。
7. **失败关闭（fail closed）**：任何未定义状态、边界值、存储错误或文件系统异常都不得退化为自动回收。

## 3. 术语与数据边界

### 3.1 目录角色

- `baselineRoot`：唯一的基准目录 A。
- `comparisonRoots[]`：至少一个对比目录，数量不固定。
- `recycleRoot`：每个对比目录下的 ImageKeeper 专用回收目录，默认 `.recycle/`。
- `reportRoot`：本次运行的报告输出目录，不得参与扫描。

界面可以显示 A/B/C 等易读别名，但内部流程必须使用角色字段，不能依赖目录选择顺序推断角色。

### 3.2 运行快照

每次分析创建唯一 `runId`，并保存不可变运行配置：

- A 和所有对比目录的规范化路径及显示别名。
- 应用版本、算法配置 ID、算法版本和依赖版本摘要。
- 哈希算法、感知哈希参数、图像归一化参数和相似度阈值。
- 支持格式、符号链接策略、排除目录和并发设置。
- 开始时间、结束时间、任务状态和错误统计。

运行中的设置修改只影响下一次运行，不得改变已生成结果的含义。

### 3.3 文件清单（manifest）

每个文件至少记录：

- `sourceRole` 和 `sourceRootId`
- 规范化相对路径
- 文件大小和修改时间
- 宽、高、格式、帧处理策略
- BLAKE3 指纹
- 感知哈希及其算法版本
- 扫描、解码和特征提取状态

绝对路径用于本地执行，但报告默认使用根目录别名和相对路径，降低隐私泄露风险。

## 4. 结果模型

分析分类、审核状态和文件操作状态必须分开存储，不能共用一个 `status` 字段表达三种含义。

### 4.1 分析分类 `analysisType`

每个对比目录文件必须且只能有一个最终分析分类：

| 分类 | 含义 | 默认建议 |
|---|---|---|
| `exact_duplicate` | 与 A 中至少一个文件 BLAKE3 相同 | 可预选进入回收站 |
| `likely_compressed` | 有方向性证据表明它可能是 A 中图片的较低分辨率版本 | 人工确认 |
| `variant` | 同主题或同场景，但存在应被保留的局部/内容差异 | 保留，允许人工改判 |
| `similar_keep` | 相似但不满足低质量版本条件 | 保留 |
| `no_baseline_match` | 在本次候选策略下未匹配到 A | 保留 |
| `inconclusive` | 多候选冲突、候选截断、边界值或证据不完整 | 人工确认，禁止预选 |
| `not_evaluated` | 被取消、算法阶段未执行或格式暂不支持 | 保留，允许重试 |
| `error` | 扫描、解码、计算或持久化失败 | 保留，允许重试 |

`no_baseline_match` 不能命名为“唯一图片”，因为本流程没有证明它在 B/C 或全图库中唯一。

### 4.2 审核状态 `reviewStatus`

```text
not_required | pending | approved_for_recycle | rejected_keep
```

- `exact_duplicate` 可以在界面中默认预选，但只有用户确认后才能进入 `approved_for_recycle`。
- `likely_compressed` 默认必须是 `pending`。
- `inconclusive` 禁止批量批准，必须查看证据后逐项决定。
- 人工决定必须记录时间、原因和可选备注。

### 4.3 操作状态 `actionStatus`

```text
none
→ validating
→ prepared
→ recycled
→ restored | permanently_deleted

任意阶段还可进入：stale | failed | reconciliation_required
```

分析分类不会因为文件已回收或恢复而改变；操作历史使用独立事件记录。

## 5. 完整流程

### Phase 0：目录预检

在读取图片前完成以下检查：

1. A 存在、可读，并明确标记为受保护目录。
2. 至少选择一个对比目录；每个目录存在且可读。
3. 对所有路径执行规范化和真实路径解析。
4. A、各对比目录不得相同、互相嵌套或通过链接指向同一位置。
5. 各对比目录之间默认也不得相同或嵌套，避免重复计数和操作冲突。
6. `.recycle`、报告目录、缓存目录和临时文件目录必须加入排除列表。
7. 默认不跟随目录符号链接；若未来允许，必须做文件身份去重和越界检查。
8. 检查报告目录可写、回收站可创建，并预估索引所需空间。

任一安全检查失败时不得开始运行。非安全型告警可以由用户确认后继续，但必须写入运行快照。

### Phase 1：扫描与特征提取

对 A 和每个 `comparisonRoot` 独立建立 manifest：

```text
发现文件
→ 读取文件系统元数据
→ 识别格式与解码策略
→ 读取宽高/帧信息
→ 计算 BLAKE3
→ 计算 64 位感知哈希
→ 保存单文件阶段状态
```

要求：

- 固定使用 BLAKE3，不在同一索引中混用 MD5 与 BLAKE3。
- 缓存命中必须同时验证路径身份、大小、修改时间和算法版本；任一不一致即重新计算。
- 单个文件失败不能默认终止整个任务，应记录为 `error` 并继续处理其他文件。
- 任务取消时保存幂等检查点；恢复后不得重复生成结果或重复执行文件操作。
- GIF 等动画格式必须记录帧策略。v1 默认只分析第一帧，并在结果中显示限制。

### Phase 2：精确匹配

对每个对比目录执行相同逻辑：

```text
for root in comparisonRoots:
    for image in root:
        if image.BLAKE3 exists in baselineHashIndex:
            analysisType = exact_duplicate
            matches = A 中所有相同哈希文件
        else:
            进入候选筛选
```

规则：

- A 中存在多个相同哈希文件时，保留完整匹配组，并选择路径稳定排序后的第一个作为展示主匹配；不得修改 A。
- 精确重复意味着文件字节相同，但实际操作仍必须先验证 A 中至少有一个匹配文件存在且指纹未变化。
- B/C 之间的相同哈希不会在本模式下建立重复关系。

### Phase 3：候选筛选

感知哈希只负责从 A 中召回候选，不负责最终分类或删除。

```text
for root in comparisonRoots:
    for image in root 中未精确匹配的文件:
        candidates = baselinePerceptualHashIndex.search(image.perceptualHash, maxDistance)
        应用宽高比和基础可比性检查
        保留按距离排序的 Top-K 候选
```

实现约束：

- 不得在大图库中直接执行 `A × (B+C+...)` 全遍历。
- 感知哈希索引可使用 BK-tree、分桶、多索引哈希或等价的近邻查询结构。
- 初始建议 `maxDistance = 10`，边界语义为 `distance <= 10`；该值属于算法配置，不是永久常量。
- 宽高比差异使用相对误差公式，不使用模糊的“分辨率差异百分比”：

```text
aspectDiff = abs(aspectA - aspectX) / max(aspectA, aspectX)
```

- 候选过多并触发 Top-K 截断时，结果必须带 `candidateTruncated = true`；若无法可靠仲裁，则分类为 `inconclusive`，不能当作未匹配。
- 文件大小和分辨率差异不能直接证明“不相似”，不得据此跳过目标中的典型缩放/压缩图片。
- 未命中任何候选且候选查询完整时，分类为 `no_baseline_match`。

### Phase 4：规范化与相似度计算

只有候选对进入本阶段。相似度算法必须实现真正、版本固定的结构相似性或经验证的等价指标，不能把简单 MSE 映射命名为结构相似性。

#### 4.1 像素规范化协议

每个算法配置必须明确并版本化：

1. EXIF orientation 处理。
2. 色彩空间、gamma 和位深转换。
3. Alpha 通道与透明背景合成规则。
4. 动画帧选择规则。
5. 宽高比容差。
6. 缩放方向、目标尺寸、重采样算法和边界处理。
7. 结构相似性的窗口大小、通道策略和常量参数。

对于尺寸不同且宽高比可比的图片，统一到像素数较少图片的完整宽高；平局时按宽、高稳定选择目标尺寸。较大图片使用 Lanczos3 缩小，不放大较小图片。

正式任务（扫描评分与分组复核）在此基础上再按最大边长 `1600` 封顶：两张图片同为高分辨率时，归一化尺寸不会超过 1600px 长边。这一层封顶只作用于正式任务；SSIM 指标测试台与找差分图小工具保留不封顶的原始口径，便于人工核对全分辨率下的真实数值。

降采样是低通滤波，会抹掉 JPEG 伪影、噪点与锐化差异这类高频信息，使 SSIM 分数系统性升高。因此最大边长属于归一化口径的一部分，调整它必须同步升级算法配置 ID，历史结果不得与新结果混用。

当前标准 SSIM 固定使用灰度通道、11×11 高斯窗口、`sigma=1.5`、镜像边界，以及由 8 位动态范围推导的 `C1=6.5025`、`C2=58.5225`。主流程、组内比较、找差分图和指标工具必须调用同一核心入口。

无法按固定协议对齐的候选对标记为计算失败或不可比，不得生成删除建议。

#### 4.2 初始参数

以下数值只是 v1 初始审核参数，必须通过无敏感内容的公开测试集和本地私有真实样本校准：

| 参数 | 初始值 | 用途 |
|---|---:|---|
| 感知哈希最大汉明距离 | `10` | 候选召回 |
| 压缩候选结构相似性 | `>= 0.995` | 生成 `likely_compressed` 候选 |
| 变体审核下限 | `>= 0.75` | 进入变体/相似结果仲裁 |
| 宽高比相对误差 | `<= 0.5%` | 判断是否可进行缩放比较 |
| 主匹配并列差值 | `<= 0.001` | 触发多候选人工确认 |

阈值必须随 `algorithmProfileId` 保存到每个运行和每个配对结果中。调整阈值不得改变历史结果。

### Phase 5：配对分类与多候选仲裁

非精确匹配文件按以下顺序得到唯一分类：

1. 候选查询不完整、候选被截断且无法可靠仲裁，或多个候选结论冲突：`inconclusive`。
2. 解码或计算未执行：`not_evaluated`；执行失败：`error`。
3. 没有候选，或所有完整计算的候选均低于变体审核下限：`no_baseline_match`。
4. 至少一个候选满足全部有向压缩条件，并且不存在并列冲突：`likely_compressed`。
5. 内容差异证据符合变体规则：`variant`。
6. 其余达到审核下限但不满足压缩或变体条件的结果：`similar_keep`。

任何规则无法唯一落入上述类别时，统一使用 `inconclusive`。

#### 5.1 有方向性的压缩候选

对比图片 `X` 只有同时满足以下条件，才允许分类为 `likely_compressed`：

```text
X.width  < A.width
X.height < A.height
X.pixelCount < A.pixelCount
X.fileSize < A.fileSize
aspectDiff <= aspectTolerance
结构相似性 >= compressedThreshold
```

补充边界：

- 任一宽或高不小于 A 时，不得仅凭结构相似性判定 X 是 A 的压缩版。
- 分辨率相同的重新编码文件不得自动归为低质量版本，只能进入 `variant`、`similar_keep` 或 `inconclusive`。
- 文件大小只作为有方向性的辅助条件，不代表视觉质量。
- A 比 X 更小、方向矛盾、格式能力差异明显或存在 Alpha/动画语义差异时，默认保留并人工确认。

#### 5.2 变体与相似保留

- 同尺寸或近似尺寸、主题高度相似但局部内容不同的图片优先分类为 `variant`。
- 相似度达到候选范围但不满足压缩方向条件时分类为 `similar_keep`。
- 结构相似性区间不能单独证明“差分图”；最终分类还需结合尺寸、配对证据和必要的人工查看。

#### 5.3 多候选仲裁

每张对比图片先收集全部候选结果，再确定主匹配和最终分类：

1. 精确哈希匹配优先于任何相似度匹配。
2. 非精确候选按可比性、结构相似性、感知哈希距离和 A 图像分辨率稳定排序。
3. 保留全部候选证据，展示主匹配不等于丢弃其他候选。
4. 排名前两项分数接近、分类结论冲突或候选被截断时，结果为 `inconclusive`。
5. 不得根据数据库返回顺序或文件遍历顺序决定主匹配。

### Phase 6：人工复核

复核界面在本地提供：

- A 与对比图片并排查看。
- 分辨率、格式、大小、BLAKE3、感知哈希距离和结构相似性。
- 缩放方向、归一化配置和所有候选 A。
- 可选的局部放大、叠加和差异视图。
- 算法建议、风险提示和人工决定原因。

交互约束：

- A 中的图片不显示删除选择框。
- `likely_compressed` 默认不勾选。
- `inconclusive`、`not_evaluated`、`error` 和过期结果禁止批量加入回收站。
- 批量批准前显示按类别和目录分组的文件数，并明确“操作对象仅来自对比目录”。
- 导出的 HTML 报告默认不嵌入图片；本地应用内预览不写入日志，也不上传内容。

### Phase 7：执行前复验与安全回收

#### 7.1 执行前复验

对每个获准回收的文件重新检查：

1. 文件仍位于允许操作的对比目录内。
2. 真实路径未通过链接跳出目录。
3. 文件大小、修改时间和 BLAKE3 与 manifest 一致。
4. 依赖的 A 匹配文件仍存在、仍在 A 内且 BLAKE3 未变化。
5. 回收目标不存在冲突，或已生成不会覆盖现有文件的唯一目标名。

任一检查失败时设置 `actionStatus = stale`，取消该文件操作并要求重新分析。

#### 7.2 回收操作

```text
写入 prepared 操作日志
→ 创建回收目标目录
→ 将文件移动到 .recycle/<runId>/...
→ 验证目标文件
→ 写入 recycled 事件
```

要求：

- 不提供从分析结果直接永久删除的入口。
- 不覆盖回收站中的同名文件。
- 文件移动和数据库记录无法形成单一事务时，必须使用操作日志并在启动时执行 reconcile。
- 中断后应能判断文件位于原位置、回收站，还是需要人工恢复。
- 扫描器必须永久排除 `.recycle`，避免回收文件再次进入候选。

#### 7.3 恢复与永久删除

- 恢复前检查原路径冲突；存在新文件时不得覆盖，应让用户选择新位置或取消。
- 永久删除只允许作用于回收站内已记录的文件。
- 永久删除需要独立页面、明确数量和二次确认。
- v1 可以提示回收站保留期限，但不得静默或定时永久删除。
- 永久删除前先导出或确认已有完整审计记录。

### Phase 8：报告与运行完成

#### 8.1 统计口径

报告至少分别给出：

- `baselineTotal`
- `comparisonTotal`，即所有对比目录文件总数
- 每个对比目录的文件数
- 八种 `analysisType` 的数量
- 待复核、已批准、已拒绝数量
- 已回收、已恢复、永久删除、过期和操作失败数量

必须满足：

```text
comparisonTotal
= exact_duplicate
 + likely_compressed
 + variant
 + similar_keep
 + no_baseline_match
 + inconclusive
 + not_evaluated
 + error
```

A 的数量不计入对比结果分类总数。

#### 8.2 JSON 主报告

JSON 是完整、可机器读取的主报告。示例：

```json
{
  "schemaVersion": 1,
  "runId": "20260713-170000-ab12cd",
  "applicationVersion": "0.1.0",
  "algorithmProfile": {
    "id": "imagekeeper-v4-standard-ssim-capped",
    "hash": "blake3",
    "perceptualHashMaxDistance": 10,
    "compressedSsimThreshold": 0.995,
    "normalizationVersion": 2,
    "ssimMaxEdge": 1600,
    "maxWorkers": 15
  },
  "roots": {
    "baseline": "A",
    "comparisons": ["B", "C"]
  },
  "summary": {
    "baselineTotal": 1000,
    "comparisonTotal": 2000,
    "exactDuplicate": 50,
    "likelyCompressed": 30,
    "variant": 10,
    "similarKeep": 15,
    "noBaselineMatch": 1885,
    "inconclusive": 5,
    "notEvaluated": 3,
    "error": 2
  },
  "results": [
    {
      "sourceRoot": "B",
      "source": "images/example.jpg",
      "analysisType": "likely_compressed",
      "reviewStatus": "pending",
      "primaryMatch": "A:originals/example.png",
      "sourceBlake3": "...",
      "matchBlake3": "...",
      "perceptualHashDistance": 2,
      "ssim": 0.996,
      "sourceResolution": [1920, 1080],
      "matchResolution": [3840, 2160],
      "algorithmProfileId": "imagekeeper-v4-standard-ssim-capped"
    }
  ]
}
```

#### 8.3 其他导出

- CSV 用于筛选和表格处理，但必须带 `runId`、分析分类、审核状态和操作状态。
- HTML 用于离线查看，不默认嵌入缩略图或绝对路径。
- 报告文件名包含 `runId`，不得静默覆盖已有报告。
- 写报告时先写临时文件，完成并校验后再原子替换目标。
- 报告查询必须按 `runId` 隔离，不能混入其他运行或整个历史回收站的数据。

## 6. 任务状态、错误与恢复

### 6.1 任务状态

```text
pending
→ preflight
→ indexing
→ matching
→ scoring
→ resolving
→ review_pending
→ analysis_complete
→ action_in_progress
→ action_complete
```

旁路终态：

```text
completed_with_errors | paused | canceled | failed
```

暂停、取消和恢复必须由后台工作循环真实响应，不能只修改数据库状态。

### 6.2 错误处理

- 文件级错误：记录阶段、错误码、可重试性和安全摘要，继续处理其他文件。
- 运行级错误：无法建立数据库、manifest 或报告时停止进入后续阶段。
- 存储空间不足：停止新的特征或回收操作，保留检查点。
- 解码器崩溃或不受支持：文件进入 `error` 或 `not_evaluated`，不得计入 `no_baseline_match`。
- 报告生成失败：分析结果可保留，但任务不能标记为完整交付。

所有重试必须幂等，同一 `runId` 不得生成重复配对、重复审核记录或重复回收事件。

## 7. 性能与扩展边界

- BLAKE3 使用哈希索引，精确匹配目标复杂度接近 O(N)。
- 感知哈希使用近邻索引，禁止在目标图库规模下默认全量笛卡尔积。
- 结构相似性只处理候选对，并记录候选数量、截断数量和实际耗时。
- 特征缓存按文件指纹和算法版本失效。
- 图片解码、pHash 和 SSIM 统一进入 4 线程有界算法池；并行计算不得共享不安全的数据库连接，计算结果在单一持久化边界串行写入。
- 并发只影响性能，不能改变候选排序、分类结果或主匹配选择。
- 进度使用稳定阶段 ID，不把感知哈希阶段显示为错误的 Phase 编号。

性能基准必须分别报告冷缓存、热缓存、文件格式、平均分辨率、存储介质和线程数，不能用单一“每张 50ms”作为产品承诺。

## 8. 隐私与敏感内容

- 所有分析默认在本地完成，不上传图片、缩略图、特征或报告。
- 日志不得包含图片像素、缩略图、完整 BLAKE3 或未经用户允许的绝对路径。
- 导出报告默认使用根目录别名和相对路径。
- 私有真实测试集不得进入 Git、云端 CI、共享报告或演示包。
- CI 只使用无敏感内容的合成图片和明确授权的公开样本。
- `.recycle`、数据库、缓存、私有 fixture 和生成报告应纳入项目忽略或用户数据目录策略。

## 9. 验证与验收标准

### 9.1 算法测试集

至少覆盖：

- 字节完全相同但文件名不同。
- 同图不同编码格式。
- 等比例缩小和不同压缩质量。
- 相同分辨率重新编码。
- 局部差分、文字覆盖、水印和轻微调色。
- 不同宽高比、裁剪、旋转和 EXIF orientation。
- Alpha、灰度、广色域、动画第一帧和损坏文件。
- 结构相似性/感知哈希阈值边界及其等号情况。
- 一张对比图片匹配多个 A 的冲突场景。

阈值回归测试必须记录算法配置和 fixture BLAKE3。任何算法、解码或缩放依赖升级都要重新运行。

### 9.2 安全流程测试

至少覆盖：

- A 与 B 相同、嵌套、链接重合时拒绝启动。
- `.recycle` 和报告目录永不参与扫描。
- 扫描后源文件或 A 文件变化时阻止回收。
- A 中任何文件都不能成为操作目标。
- 回收目标重名、移动中断、数据库写入失败后的 reconcile。
- 恢复路径已存在时不覆盖。
- 取消、暂停、恢复和重复重试保持幂等。
- 多次和并发运行的报告、结果与回收记录按 `runId` 隔离。

### 9.3 发布门槛

- 自动预选类别在验证集上必须达到零误选；无法证明时关闭自动预选。
- `likely_compressed` 在 v1 默认必须人工确认，不允许自动永久删除。
- 所有结果分类数量满足统计守恒。
- 错误、未计算、过期和冲突结果不存在可执行删除动作。
- 回收、恢复和永久删除都有完整审计记录。

## 10. 实现符合性检查清单

后续代码审查按以下清单逐项判断，不以“界面看起来能运行”作为完成标准。

### 输入与模型

- [ ] 后端接口能够表达一个 A 和任意数量的对比目录。
- [ ] 数据模型显式保存目录角色、`runId` 和不可变算法配置。
- [ ] A/B/C 路径规范化、重合和嵌套检查完整。
- [ ] manifest、结果、审核和操作状态相互独立。

### 匹配与分类

- [ ] B/C/... 使用同一套 `comparisonRoot → A` 流程。
- [ ] 固定使用 BLAKE3，感知哈希只用于候选召回。
- [ ] 实现的相似度指标与文档名称及阈值一致。
- [ ] 归一化协议固定且可版本化复现。
- [ ] 压缩候选判定保留大小方向，不使用 `min/max` 丢失方向。
- [ ] 同分辨率图片不会被自动当作低质量版本。
- [ ] 多候选冲突、Top-K 截断和计算失败均保守处理。

### 操作安全

- [ ] A 在所有命令、数据库查询和 UI 操作中不可删除。
- [ ] 回收前重新验证源文件和 A 文件指纹。
- [ ] 分析页不存在直接永久删除入口。
- [ ] `.recycle` 永久排除扫描，并按 `runId` 隔离。
- [ ] 文件系统操作具有日志、冲突策略和启动恢复机制。
- [ ] 恢复和永久删除拥有独立确认与审计事件。

### 错误、报告与测试

- [ ] 文件级失败不会被算成未匹配或唯一图片。
- [ ] 暂停、取消和恢复会被实际工作循环响应。
- [ ] JSON/CSV/HTML 按 `runId` 导出且不静默覆盖。
- [ ] 报告包含算法版本、阈值、双方指纹和人工决定。
- [ ] 分类统计满足守恒公式。
- [ ] 算法 golden tests、安全流程测试和边界测试通过。

## 11. 版本历史

- **v2.0**（2026-07-13）：重写为目标流程规范；明确 A 只读、N 个对比目录、结果状态、候选仲裁、人工复核、执行前复验、安全回收、审计与验收边界。
- **v1.0**（2026-07-13）：初始流程设计。

## 12. 参考资料

- [结构相似性算法原理](https://en.wikipedia.org/wiki/Structural_similarity)
- [感知哈希介绍](http://www.hackerfactor.com/blog/index.php?/archives/432-Looks-Like-It.html)
- [汉明距离](https://en.wikipedia.org/wiki/Hamming_distance)
